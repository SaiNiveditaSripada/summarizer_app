# Core libraries
streamlit>=1.28.0
torch>=1.12.0
transformers>=4.30.0
PyMuPDF>=1.21.0
regex>=2021.4.4

# Optional (for stability)
requests>=2.25.1

# Speedup for tokenization (optional but recommended)
sentencepiece>=0.1.99
protobuf<4.0.0  # to avoid compatibility issues with older transformers

# Pin numpy to avoid mismatched torch/transformers behavior
numpy>=1.21.0
